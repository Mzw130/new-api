# -----------------------------------------------------------------------------
# Docker Desktop 若要「每个容器单独一个 Compose 项目」，请用：
#   .\scripts\compose-instances-up.ps1 -Build
#
# 合并单栈（一个目录下全在）：docker compose -f docker-compose.monolith.yml up -d --build
# 说明：deploy/compose-instances/README.md
# -----------------------------------------------------------------------------

name: new-api-entry

services:
  # 无默认服务；避免误 `docker compose up` 拉起整栈。
  _placeholder:
    image: alpine:3.19
    command: ["true"]
    profiles: ["unused"]
