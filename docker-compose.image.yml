# Run the production-style image (Dockerfile) with the same env style as docker-compose.dev.yml.
#
# Build (does NOT read this file — only Dockerfile.api-only for split):
#   docker build -f Dockerfile.api-only -t 1router:api-only .
#
# Configure secrets locally (never commit .env.deploy):
#   copy .env.deploy.example -> .env.deploy and edit values
#
# Start:
#   docker compose -f docker-compose.image.yml up -d
#
# Stop:
#   docker compose -f docker-compose.image.yml down


name: 1router

services:
  new-api:
    build:
      context: .
      dockerfile: Dockerfile.api-only
    image: 1router:api-only
    container_name: 1router
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    env_file:
      - .env.deploy
